categories:
- Database
- MySQL
questions:
- variable: persistence.enabled
  default: "false"
  description: "Wil je dat jouw data in deze applicatie opgeslagen wordt nadat de applicatie crasht of verwijderd wordt? Let op: Dit gaat van jouw beschikbare opslag af."
  type: boolean
  required: true
  label: Persistent Volume Enabled
  show_subquestion_if: true
  group: "Persistent Volume"
  subquestions:
  - variable: persistence.size
    default: "8Gi"
    description: "Volume Size"
    type: string
    label: Volume Size
    required: true
  - variable: persistence.storageClass
    default: ""
    description: "If undefined or null, uses the default StorageClass. Defaults to null."
    type: storageclass
    label: Default storageClass

- variable: service.nodePort
  default: ""
  description: "Om jouw specifieke applicatie te bereiken is een uniek poortnummer nodig. Kies een poortnummer tussen 30000-32767."
  type: int
  min: 30000
  max: 32767
  label: Service NodePort number
